# 🚀 5分钟部署到 Streamlit Cloud - 快速指南

## 准备工作已完成 ✅

- [x] Git仓库已初始化
- [x] 代码已提交
- [x] 分支已重命名为main

---

## 步骤1：在GitHub创建仓库（2分钟）

### 1.1 创建新仓库

1. 访问：https://github.com/new
2. 填写信息：
   - **Repository name**: `deepread-app`（或其他你喜欢的名字）
   - **Description**: DeepRead 深度阅读应用
   - **Public/Private**: 建议选择 **Public**（免费部署）
   - **不要勾选** "Add a README file"
   - **不要勾选** "Add .gitignore"
3. 点击 **"Create repository"**

### 1.2 推送代码到GitHub

创建仓库后，GitHub会显示一些命令。**复制并运行以下命令**（替换`YOUR_USERNAME`为你的GitHub用户名）：

```bash
# 在命令行中运行（deepread目录下）
cd c:\Users\黎又榜\每日新闻推送系统\deepread

# 添加远程仓库（替换YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/deepread-app.git

# 推送代码
git push -u origin main
```

**示例：**
如果你的GitHub用户名是 `zhangsan`，命令是：
```bash
git remote add origin https://github.com/zhangsan/deepread-app.git
git push -u origin main
```

---

## 步骤2：部署到 Streamlit Cloud（3分钟）

### 2.1 登录 Streamlit Cloud

1. 访问：https://streamlit.io/cloud
2. 点击右上角 **"Sign up"** 或 **"Log in"**
3. 使用 **GitHub账号登录**（授权即可）

### 2.2 部署应用

登录后：

1. 点击 **"New app"** 按钮
2. 填写部署信息：
   - **Repository**: 选择 `deepread-app`（你刚创建的仓库）
   - **Branch**: `main`
   - **Main file path**: 点击文件夹图标，找到并选择 `app_v3.8.py`
3. 点击 **"Deploy"** 按钮

### 2.3 等待部署

- ⏱ 首次部署需要 **1-3分钟**（安装依赖）
- 📊 可以看到实时部署日志
- ✅ 部署成功后会显示绿色对勾

---

## 步骤3：获取分享链接（30秒）

部署成功后，你会看到一个URL：

```
https://deepread-app.streamlit.app
```

**如果仓库名被占用，URL可能是：**
```
https://deepread-app-xyz123.streamlit.app
```

**这个链接可以：**
- ✅ 直接分享到微信
- ✅ 直接分享到小红书
- ✅ 手机扫码访问
- ✅ 任何人都能打开

---

## 步骤4：测试和分享（1分钟）

### 4.1 测试访问

1. 打开你的应用URL
2. 测试功能：
   - 选择一本书
   - 生成金句卡片
   - 生成阅读海报
   - 复制分享文案

### 4.2 分享到微信

**方式1：直接分享链接**
```
复制链接：https://your-app.streamlit.app
粘贴到微信聊天发送
```

**方式2：生成二维码**
```
在电脑浏览器打开链接
按 Ctrl+P 打印
选择 "另存为PDF" 或 "打印到图片"
然后分享图片或二维码
```

### 4.3 分享到小红书

**图文形式：**
1. 截图应用界面（金句卡片、阅读海报都很漂亮）
2. 配文：
```
💡 用DeepRead深度阅读
每天进步一点点

📚 沉浸式阅读体验
✨ 自动生成金句卡片
📊 阅读数据可视化

🔗 链接在评论区
#深度阅读 #读书 #自我提升 #工具分享
```
3. 发布后在评论区添加链接

---

## 🎉 完成！

现在你已经有了一个可以分享到微信和小红书的深度阅读应用了！

---

## 常见问题

### Q1: Git推送失败？

**如果提示 "Permission denied"：**
- 检查GitHub用户名是否正确
- 确认仓库是否创建成功
- 检查是否有GitHub账号的写入权限

### Q2: Streamlit Cloud部署失败？

**检查以下几点：**
1. 确保代码已推送到GitHub
2. 检查 `app_v3.8.py` 在仓库根目录
3. 查看部署日志（点击红色的 "View logs"）

**常见错误：**
- **Module not found**: 检查 `requirements.txt` 是否包含所有依赖
- **File not found**: 确保 `app_v3.8.py` 路径正确

### Q3: 如何更新应用？

**修改代码后：**
```bash
# 1. 提交修改
git add .
git commit -m "描述你的修改"

# 2. 推送到GitHub
git push

# 3. Streamlit Cloud会自动检测并重新部署
# 不需要手动操作！
```

### Q4: 可以修改应用名称吗？

**可以！有两种方式：**

**方式1：在 Streamlit Cloud 中**
1. 进入应用设置
2. 点击 "Settings"
3. 修改 "Custom subdomain"

**方式2：重新部署**
1. 修改仓库名
2. 重新部署到Streamlit Cloud

### Q5: 免费版够用吗？

**Streamlit Cloud 免费额度：**
- ✅ 每月500小时运行时间（约16小时/天）
- ✅ 适合个人使用和小团队
- ✅ 无限应用数量
- ✅ 自动HTTPS

**如果不够用：**
- 付费版：$30/月，无限运行时间
- 或考虑自建服务器

---

## 下一步优化（可选）

### 1. 添加自定义域名

如果你有自己的域名（如 `reading.yourdomain.com`）：

1. 在 Streamlit Cloud 应用设置中
2. 点击 "Settings" → "Domains"
3. 添加自定义域名
4. 在域名提供商（阿里云/腾讯云）添加CNAME记录

### 2. 添加访问密码

如果不想让所有人访问：

在 `app_v3.8.py` 开头添加：

```python
import streamlit as st

def check_password():
    def password_entered():
        if st.session_state["password"] == "你的密码":
            st.session_state["password_correct"] = True
            del st.session_state["password"]
        else:
            st.session_state["password_correct"] = False

    if "password_correct" not in st.session_state:
        st.text_input("密码", type="password", on_change=password_entered, key="password")
        return False

    if not st.session_state["password_correct"]:
        st.error("密码错误")
        return False

    return True

if not check_password():
    st.stop()  # 停止执行
```

### 3. 配置环境变量

如果使用API密钥：

1. 在 Streamlit Cloud 应用设置中
2. 点击 "Settings" → "Secrets"
3. 添加环境变量

代码中访问：
```python
api_key = st.secrets["API_KEY"]
```

---

## 📞 需要帮助？

- **Streamlit Cloud文档**: https://docs.streamlit.io/streamlit-cloud
- **GitHub文档**: https://docs.github.com
- **常见问题**: https://docs.streamlit.io/streamlit-cloud/get-started/deploy-your-app

---

**准备好了吗？开始部署吧！** 🚀

1. 创建GitHub仓库
2. 推送代码
3. 在Streamlit Cloud部署
4. 分享链接

**整个过程不到5分钟！**
