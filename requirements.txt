# DeepRead 深度阅读工具 - 依赖包

# Web框架
streamlit>=1.28.0

# HTTP请求
requests>=2.31.0

# 向量数据库
chromadb>=0.4.18

# 文本嵌入（本地模型）
sentence-transformers>=2.2.2

# 文字转语音（免费）
edge-tts>=6.1.9

# 音频处理（可选，用于合并音频）
pydub>=0.25.1

# 图像生成（更好中文支持）
matplotlib>=3.7.0

# 数据处理
python-dotenv>=1.0.0

# Groq API客户端（可选，也可以直接用requests）
groq>=0.4.1

# 日志
loguru>=0.7.2

# 时间处理
python-dateutil>=2.8.2

# 云端同步服务器
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# 图像处理（用于封面生成）
Pillow>=10.0.0

# 可选：导出功能
reportlab>=4.0.0
python-docx>=0.8.11
